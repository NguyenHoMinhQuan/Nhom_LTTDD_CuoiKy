<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="@color/white">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- Hình nhỏ bên cạnh -->
        <ImageView
            android:id="@+id/ivAnnouncementIcon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/mon1"
            android:contentDescription="Icon thông báo"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

        <!-- Tiêu đề thông báo -->
        <TextView
            android:id="@+id/tvAnnouncementTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Lịch nghỉ Tết"
            android:textStyle="bold"
            android:textSize="16sp"
            android:textColor="@color/text_title"
            android:layout_marginStart="8dp"
            app:layout_constraintStart_toEndOf="@id/ivAnnouncementIcon"
            app:layout_constraintTop_toTopOf="@id/ivAnnouncementIcon"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Nội dung thông báo -->
        <TextView
            android:id="@+id/tvAnnouncementBody"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Trường sẽ nghỉ Tết theo lịch Bộ GD."
            android:textSize="14sp"
            android:textColor="@color/text_body"
            android:layout_marginTop="4dp"
            app:layout_constraintStart_toStartOf="@id/tvAnnouncementTitle"
            app:layout_constraintTop_toBottomOf="@id/tvAnnouncementTitle"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Ngày tạo -->
        <TextView
            android:id="@+id/tvAnnouncementDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="25/12/2025"
            android:textSize="12sp"
            android:textColor="@color/black"
            android:layout_marginTop="6dp"
            app:layout_constraintStart_toStartOf="@id/tvAnnouncementBody"
            app:layout_constraintTop_toBottomOf="@id/tvAnnouncementBody"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>